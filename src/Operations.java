public class Operations {
	//Creating a memory object that points to the singleton
	Memory myMemory = Memory.getInstance();
	
	//Creating a register object that points to the singleton
	Register myRegister = Register.getInstance();
	
//Load Register from Memory
	//Regular method with Indirect bit
	public static void loadRegister(int reg, int indexReg, int address, int indirect)
	{
		
	}
	
	//Overloading method without Indirect bit
	public static void loadRegister(int reg, int indexReg, int address)
	{
		
	}
	
//Load
	
}
